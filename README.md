Crea una clase `Empleado` que implemente `Serializable` y tenga atributos como `nombre`, `id` y `salario`. Escribe un programa que guarde una lista de empleados en un archivo usando serialización y 
luego los lea desde el archivo.
